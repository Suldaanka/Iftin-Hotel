/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "images.pexels.com",
        "localhost:3000",
        "api.iftinhotel.com"
      ],
    },
  };
  
  export default nextConfig;
  